# GePT Web Configuration
# Copy to .env.local for local development
# Add these to Vercel Environment Variables for production

# ===========================================
# REQUIRED - Database
# ===========================================
# Neon Postgres connection string (use pooled connection for serverless)
DATABASE_URL=postgresql://user:password@host/database?sslmode=require

# ===========================================
# REQUIRED - Authentication
# ===========================================
# Secret key for signing tokens (min 32 characters)
# Generate with: openssl rand -base64 32
BETTER_AUTH_SECRET=your-secret-key-min-32-chars

# Base URL for auth callbacks
# Local: http://localhost:3000
# Production: https://gept.gg
BETTER_AUTH_URL=http://localhost:3000

# Public app URL (used by client-side auth)
PUBLIC_APP_URL=http://localhost:3000

# ===========================================
# REQUIRED - Site Configuration
# ===========================================
# Site URL for SEO meta tags and canonical URLs
SITE=https://gept.gg

# ===========================================
# OPTIONAL - Email (Resend)
# ===========================================
# Resend API key for transactional emails (password reset, etc.)
# Sign up at https://resend.com and verify your domain
# Without this, password reset links are logged to console only
RESEND_API_KEY=re_your_api_key_here

# From address for outbound emails (must match verified Resend domain)
EMAIL_FROM=GePT <noreply@gept.gg>

# ===========================================
# OPTIONAL - External Services
# ===========================================
# Prediction API endpoint (uses mock data if not set)
PREDICTION_API=http://your-ampere-server:8000

# ===========================================
# REQUIRED FOR ACTIVE-TRADE ALERTS - Webhook Configuration
# ===========================================
# Webhook endpoint on ML engine for trade events
ENGINE_WEBHOOK_URL=http://your-ampere-server:8000/webhooks/trades

# Shared secret for webhook signature verification (min 32 characters)
# Generate with: openssl rand -base64 32
WEBHOOK_SECRET=your-webhook-secret-min-32-chars
