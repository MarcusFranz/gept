{
  "name": "@gept/engine",
  "version": "1.0.0",
  "description": "GePT Recommendation Engine - FastAPI backend for OSRS trading predictions",
  "private": true,
  "scripts": {
    "dev": "uvicorn src.api:app --reload --host 0.0.0.0 --port 8000",
    "start": "uvicorn src.api:app --host 0.0.0.0 --port 8000",
    "test": "pytest",
    "test:cov": "pytest --cov=src --cov-report=term-missing",
    "lint": "flake8 src tests",
    "format": "black src tests"
  },
  "engines": {
    "node": ">=18"
  }
}
