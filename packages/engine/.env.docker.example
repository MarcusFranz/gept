# Example env file for running the engine via Docker Compose locally.
#
# Usage:
#   cd packages/engine
#   cp .env.docker.example .env.docker
#   docker compose -f docker-compose.local.yml --env-file .env.docker up --build
#
# IMPORTANT:
# - If you have an SSH tunnel on your host (port 5432), use host.docker.internal
#   so the container can reach it.
# - For a "just run the API" local dev setup, keep TRADE_WEBHOOKS_ENABLED and
#   PRICE_DROP_MONITOR_ENABLED disabled to avoid needing webhook config.

# Predictions DB (often reached via SSH tunnel on host)
DB_CONNECTION_STRING=postgresql://user:password@host.docker.internal:5432/osrs_data

# Required: API key for X-API-Key auth
INTERNAL_API_KEY=replace-with-random-token

# Optional: Redis cache
# REDIS_URL=redis://redis:6379/0

# Disable production integrations by default for local runs
TRADE_WEBHOOKS_ENABLED=false
PRICE_DROP_MONITOR_ENABLED=false

# Required only when TRADE_WEBHOOKS_ENABLED=true or PRICE_DROP_MONITOR_ENABLED=true
# WEBHOOK_SECRET=replace-with-random-token
# WEB_APP_WEBHOOK_URL=https://your-web-app.example/api/webhooks/alerts
# WEB_APP_RESYNC_URL=https://your-web-app.example/api/trades/resync

LOG_FORMAT=text
LOG_LEVEL=INFO

