version: '3.8'

services:
  prediction-engine:
    build: .
    ports:
      - "8000:8000"
    environment:
      - DB_CONNECTION_STRING=${DB_CONNECTION_STRING}
      - DISCORD_WEBHOOK_URL=${DISCORD_WEBHOOK_URL}
      - MODEL_REGISTRY_PATH=models/registry.json
      - MIN_EV_THRESHOLD=0.001
      - PREDICTION_INTERVAL_MINUTES=5
      - LOG_LEVEL=INFO
    volumes:
      - ./models:/app/models:ro
      - ./logs:/app/logs
    restart: unless-stopped
    healthcheck:
      test: ["CMD", "curl", "-f", "http://localhost:8000/api/v1/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 15s

  # Optional: Add a scheduler service if running predictions in separate process
  # scheduler:
  #   build: .
  #   command: python -m src.run_scheduler
  #   environment:
  #     - DB_CONNECTION_STRING=${DB_CONNECTION_STRING}
  #     - DISCORD_WEBHOOK_URL=${DISCORD_WEBHOOK_URL}
  #     - MODEL_REGISTRY_PATH=models/registry.json
  #   volumes:
  #     - ./models:/app/models:ro
  #     - ./logs:/app/logs
  #   restart: unless-stopped
  #   depends_on:
  #     - prediction-engine
