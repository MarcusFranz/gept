# =============================================================================
# GePT Server Configuration
# =============================================================================
# Central configuration for all server-related settings.
# Source this file in deployment scripts.
#
# Override via environment variables or .env file:
#   export AMPERE_HOST="ubuntu@new.server.ip"
#   ./deploy_ampere.sh
#
# Or set in your local .env file (git-ignored):
#   AMPERE_HOST=ubuntu@192.168.1.100
# =============================================================================

# Ampere server (production inference + data collection)
AMPERE_HOST="${AMPERE_HOST:-ubuntu@150.136.170.128}"
AMPERE_IP="${AMPERE_IP:-150.136.170.128}"
AMPERE_SSH_KEY="${AMPERE_SSH_KEY:-.secrets/oracle_key.pem}"

# Remote directories
AMPERE_GEPT_DIR="${AMPERE_GEPT_DIR:-/home/ubuntu/gept}"
AMPERE_COLLECTOR_DIR="${AMPERE_COLLECTOR_DIR:-/home/ubuntu/osrs_collector}"

# Service ports (for documentation/reference)
DASHBOARD_PORT="${DASHBOARD_PORT:-8080}"
PROMETHEUS_PORT="${PROMETHEUS_PORT:-9090}"
GRAFANA_PORT="${GRAFANA_PORT:-3001}"
ALERTMANAGER_PORT="${ALERTMANAGER_PORT:-9093}"
