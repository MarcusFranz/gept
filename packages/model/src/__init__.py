"""
GePT Model - GE Flipping Prediction System

A production-ready prediction system for OSRS Grand Exchange flipping.
"""

__version__ = "1.0.0"

# Lazy imports to avoid circular dependencies and import errors
# when running submodules directly. Import specific modules as needed:
#   from src.feature_engine import FeatureEngine
#   from src.db_utils import get_db_cursor
