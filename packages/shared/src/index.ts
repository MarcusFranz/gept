/**
 * @gept/shared
 *
 * Shared types, configuration, and utilities for GePT packages.
 *
 * Usage:
 *   import { Recommendation, UserSettings } from '@gept/shared';
 *   import { CACHE_TTL, RATE_LIMITS } from '@gept/shared/config';
 */

export * from './types';
export * from './config';
